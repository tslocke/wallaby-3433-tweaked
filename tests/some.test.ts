describe('test something', () => {
  test('whatever', () => {
    expect(1+2).toBe(3)
  })
  test.onl('something else', () => {
    expect(1+2).toBe(3)
  })
  test('another', () => {
    expect(1+2).toBe(3)
  })
})
